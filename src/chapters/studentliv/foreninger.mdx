---
order: 2090
chapter: 25
title: Foreninger tilknyttet Ifi
short: Foreninger
license: CC BY-NC 4.0
previous: /studentliv/kvinneandelen
next: /personer
---

import { graphql } from 'gatsby'
import Associations from "../../components/associations"
import ChapterLayout from "../../components/chapter-layout"
import Footnote from '../../components/footnote'

<p>
  Det er etterhvert mange foreninger som er eller har vært aktive på Ifi eller har en sterk tilknytning til
  instituttet. Dette er et forsøk på å liste opp dem man vet om<Footnote>Hentet fra [Ifi-ordenen sine
  sider](http://ordenen.ifi.uio.no/association/) og med hjelp fra aktive i CYB</Footnote>.
</p>

<Associations active={true} data={props.data.allForeningerCsv}/>

## Ikke lenger aktive

<Associations active={false} data={props.data.allForeningerCsv}/>

export const associationsQuery = graphql`
  query {
    allForeningerCsv(sort: {name: ASC}) {
      edges {
        node {
          active
          name
          description
        }
      }
    }
  }
`

export default function Layout(props) {
  return <ChapterLayout {...props} />
}
