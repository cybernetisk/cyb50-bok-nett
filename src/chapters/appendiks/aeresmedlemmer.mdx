---
order: 10030
title: Æresmedlemmer
previous: /appendiks/kjellerstyrer
next: /appendiks/domener
---

import { graphql } from 'gatsby'
import HonoraryMembers from "../../components/honorary-members"
import ChapterLayout from "../../components/chapter-layout"

Liste over personer som har fått CYBs æresmedlemskap.

<HonoraryMembers data={props.data.allAeresmedlemmerCsv} />

export const honoryMembersQuery = graphql`
  query {
    allAeresmedlemmerCsv {
      edges {
        node {
          name
          date
        }
      }
    }
  }
`

export default function Layout (props) {
  return <ChapterLayout {...props} />
}
