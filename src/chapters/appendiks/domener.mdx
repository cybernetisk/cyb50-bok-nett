---
order: 10040
title: Domener
previous: /appendiks/aeresmedlemmer
next: /appendiks/oppskrifter
---

import { graphql } from 'gatsby'
import Domains from "../../components/domains"
import ChapterLayout from "../../components/chapter-layout"

Liste over domener som CYB eier.

<Domains data={props.data.allDomenerCsv} />

export const honoryMembersQuery = graphql`
  query {
    allDomenerCsv {
      edges {
        node {
          url
          description
        }
      }
    }
  }
`

export default function Layout (props) {
  return <ChapterLayout {...props} />
}
